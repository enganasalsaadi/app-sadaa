import type { SpacingToken } from '@/core/theme/types';

/** Default horizontal padding for `Layout` content when `contentPadding` is true */
export const LAYOUT_DEFAULT_PX: SpacingToken = 'lg';
/** Default vertical padding for `Layout` content when `contentPadding` is true */
export const LAYOUT_DEFAULT_PY: SpacingToken = 'lg';
