/** Visual height of the floating bar content (excluding safe-area bottom inset). */
export const FLOATING_BAR_HEIGHT = 100;

/**
 * Extra bottom clearance to add to scroll content so the last item
 * is never obscured by the floating bar.
 * = bar height + breathing room (16px)
 */
export const FLOATING_BAR_CLEARANCE = FLOATING_BAR_HEIGHT + 16;
